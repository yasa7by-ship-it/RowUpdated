# كيفية تشغيل الموقع 🚀

## المتطلبات الأساسية

- **Node.js** (الإصدار 18 أو أحدث)
- **npm** أو **yarn** أو **pnpm**

---

## خطوات التشغيل

### 1. الانتقال إلى مجلد المشروع
```bash
cd ROWDB-main
```

### 2. تثبيت المكتبات المطلوبة
```bash
npm install
```

أو إذا كنت تستخدم **yarn**:
```bash
yarn install
```

أو **pnpm**:
```bash
pnpm install
```

### 3. إنشاء ملف المتغيرات البيئية (اختياري)
إذا كان المشروع يحتاج إلى متغيرات بيئية، أنشئ ملف `.env` في مجلد `ROWDB-main`:

```env
# Gemini API Key (اختياري - فقط إذا كنت تستخدم ميزات Gemini)
GEMINI_API_KEY=your_gemini_api_key_here
```

**ملاحظة**: إذا لم يكن لديك Gemini API Key، يمكنك تخطي هذه الخطوة. الموقع سيعمل بدونها.

### 4. تشغيل الموقع في وضع التطوير
```bash
npm run dev
```

أو:
```bash
yarn dev
```

أو:
```bash
pnpm dev
```

### 5. فتح المتصفح
بعد تشغيل الأمر، ستظهر رسالة مثل:
```
  VITE v6.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
```

افتح المتصفح على العنوان: **http://localhost:3000**

---

## الأوامر المتاحة

### `npm run dev`
- يشغل الموقع في وضع التطوير
- يعمل على `http://localhost:3000`
- يعيد التحميل تلقائياً عند تغيير الكود

### `npm run build`
- يبني نسخة الإنتاج من الموقع
- الملفات النهائية في مجلد `dist/`

### `npm run preview`
- يعرض نسخة الإنتاج محلياً
- يُستخدم بعد `npm run build`

---

## استكشاف الأخطاء

### مشكلة: "command not found: npm"
**الحل**: تأكد من تثبيت Node.js من [nodejs.org](https://nodejs.org/)

### مشكلة: "Cannot find module"
**الحل**: قم بتشغيل `npm install` مرة أخرى

### مشكلة: Port 3000 مستخدم
**الحل**: 
1. أغلق البرنامج الذي يستخدم البورت 3000
2. أو غيّر البورت في `vite.config.ts` من 3000 إلى رقم آخر (مثلاً 3001)

### مشكلة: خطأ في الاتصال بـ Supabase
**الحل**: تأكد من أن:
- اتصال الإنترنت يعمل
- بيانات Supabase في `services/supabaseClient.ts` صحيحة

---

## معلومات إضافية

- **المصدر الرئيسي**: `index.tsx`
- **الصفحة الرئيسية**: `App.tsx`
- **إعدادات Vite**: `vite.config.ts`
- **إعدادات Supabase**: `services/supabaseClient.ts`

---

## بعد التشغيل

1. ✅ افتح المتصفح على `http://localhost:3000`
2. ✅ سترى الصفحة الرئيسية (Landing Page)
3. ✅ يمكنك تسجيل الدخول أو إنشاء حساب جديد
4. ✅ بعد تسجيل الدخول، ستنتقل تلقائياً إلى صفحة تحليل الأسهم

---

**استمتع بالموقع! 🎉**








